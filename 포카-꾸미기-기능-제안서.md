# 포카 꾸미기 기능 제안서

마이 스타 캘린더 · 포토카드 데코레이션

작성일: 2026-06-30

---

## 한 줄 요약

포카 위에 스티커·텍스트(이후 그림까지)를 올려 꾸미고, 그 결과를 **원본 사진을 건드리지 않는 수정 가능한 레이어**로 저장한다. 앞면·뒷면을 각각 따로 꾸밀 수 있고, 언제든 다시 옮기거나 지울 수 있다.

---

## 왜 이 방식인가

- **비파괴 저장**: 꾸민 내용을 원본 이미지에 합쳐버리면 나중에 수정·삭제가 불가능하다. 스티커의 위치·크기·각도를 데이터로만 저장하면 무한히 다시 편집할 수 있다.
- **앞/뒷면 분리**: 양면 포카가 이미 있으니, 데코도 면별로 따로 보관해야 자연스럽다.
- **공유는 합쳐서**: 평소엔 레이어로 들고 있다가, 공유·내보내기 할 때만 캔버스로 한 장 PNG로 합성한다.

---

## 데이터 구조 (수정 가능한 레이어)

포토카드 객체(`p`)에 `deco` 필드를 추가한다. 기존 필드(`img`, `imgBack`, `effect`, `name`, `qty`, `status`)는 그대로 둔다.

```
p.deco = {
  front: [ <레이어>, <레이어>, ... ],
  back:  [ <레이어>, ... ]
}
```

레이어 한 개 예시:

```
// 스티커(이모지/이미지)
{ id, type: "sticker", kind: "emoji", content: "💖",
  x: 0.5, y: 0.3, scale: 1.0, rot: -8, z: 1 }

// 텍스트
{ id, type: "text", text: "최애 ♡",
  x: 0.5, y: 0.8, scale: 1.0, rot: 0,
  color: "#ff5ec4", font: "rounded", z: 2 }

// 그림 (2단계)
{ id, type: "draw", png: "<dataURL 또는 stroke 좌표 배열>", z: 0 }
```

핵심 규칙: **좌표(x, y)는 카드 크기 대비 0~1 비율로 저장**한다. 그래야 바인더의 작은 카드든 상세보기의 큰 카드든 같은 위치에 똑같이 보인다.

---

## 기능 범위 (단계별)

### 1단계 — 스티커 + 텍스트 (추천 시작점)

- 이모지·이미지 스티커를 카드 위에 올리고 끌어 옮기기
- 두 손가락(핀치)·휠로 크기 조절, 회전 핸들로 회전
- 짧은 텍스트 추가 (색·글꼴 몇 종)
- 레이어 선택 → 삭제 / 맨 앞·뒤로 보내기
- 앞면·뒷면 각각 편집

기존 코드를 거의 그대로 재활용할 수 있어 가장 빠르고 안정적이다.

### 2단계 — 그림 그리기 (이후)

- 펜 자유 드로잉: 색·굵기·지우개·되돌리기
- 모바일 성능과 저장 처리가 까다로워 별도 단계로 분리

---

## 재활용할 기존 코드

| 필요한 것 | 이미 있는 것 |
|---|---|
| 끌어 옮기기·핀치 확대 제스처 | 크롭 편집기, 포카 위치 편집기(`휠·손가락 모으기로 크기`) |
| 이모지 스티커 목록 | `STICKERS` 배열 (현재 달력 날짜용) |
| 카드 위 오버레이 레이어 구조 | 반짝이 FX 레이어(`pocaFxHtml`, `.poca-fx`) |
| 앞/뒤 카드 렌더 + 뒤집기 | `.poca-flip` / `.poca-flip-inner` / `.poca-face` |
| 캔버스로 이미지 합성·내보내기 | 커버 캔버스 그리기 헬퍼, `pcShare`(공유) |

즉, "스티커를 절대 위치 레이어로 얹는다"는 핵심은 FX 레이어가 이미 하고 있고, "끌어서 옮기고 핀치로 키운다"는 크롭 편집기가 이미 하고 있다. 둘을 합치는 작업에 가깝다.

---

## 편집 화면(UX) 흐름

1. 포카 상세 또는 바인더에서 **"꾸미기"** 진입
2. 카드를 크게 띄우고, 하단에 도구 트레이(스티커 / 텍스트 / (2단계)그림 / 앞·뒤 전환)
3. 스티커를 탭하면 카드 중앙에 추가 → 끌어 옮기고, 모서리 핸들로 크기·회전
4. 레이어를 탭해 선택 → 삭제·복제·순서 변경
5. **저장**하면 `p.deco`에 반영, 바인더·상세·홈 어디서나 동일하게 표시
6. **공유** 시에만 원본 + 데코를 캔버스에 합성해 PNG로 내보내기

---

## 표시(렌더) 방식

- 카드의 각 면(`.poca-face`) 안에 데코 오버레이 컨테이너를 하나 둔다.
- 레이어마다 절대 배치 + `transform: translate(x,y) scale(s) rotate(r)`.
- 0~1 비율 좌표라 바인더(작은 카드)·상세(큰 카드) 모두 자동으로 맞는다.
- 반짝이 FX와 함께 보일 때의 순서(겹침)는 데코가 FX 위/아래 중 어디인지 한 번 정해두면 된다 → **결정 필요 항목**.

---

## 짚어둘 점 / 결정할 것

- **데코와 반짝이 효과의 겹침 순서**: 스티커가 글리터 위로 보이게 할지, 아래로 깔지.
- **바인더에서 바로 편집 vs 상세에서만 편집**: 작은 바인더 카드에선 보기만 하고, 편집은 큰 화면에서 하는 쪽이 조작이 편하다(추천).
- **이미지 스티커 용량**: 사용자가 직접 올린 이미지 스티커를 허용하면 저장 용량이 커진다. 처음엔 기본 제공 이모지·도형 스티커로 시작하는 걸 추천.
- **공유 PNG 해상도**: 원본 사진 해상도 기준으로 합성.

---

## 대략적인 작업량

- 1단계(스티커+텍스트, 비파괴 레이어, 앞/뒤, 편집 모달, 렌더, 공유 합성): 중간 규모. 제스처·스티커·오버레이 코드가 이미 있어 새로 짤 양은 생각보다 적음.
- 2단계(그림): 별도 규모. 1단계 안정화 후 착수 권장.

---

*다음 단계: 위 "결정할 것"만 정해지면 1단계부터 바로 구현 들어갈 수 있어요.*
