# 업데이트할 때 하는 일 (3단계)

새 기능을 올리거나 소식을 알릴 때마다 아래 3가지만 하면 돼요.
순서대로 한 뒤 VS Code에서 커밋 → 푸시하면 끝.

---

## 1. 공지사항 추가 — `script/app.js`

파일 위쪽에서 `const NOTICES = [` 를 찾아, **맨 위에** 한 줄 추가:

```js
{ id: 3, cat: "update", date: "2026.07.01", title: "제목", body: "내용" },
```

- **id** : 이전보다 큰 숫자 (지금 최대가 `2` → 다음은 `3`). 이 숫자가 커져야 사용자에게 빨간 점이 떠요.
- **cat** : `"update"`(업데이트) · `"info"`(안내) · `"maint"`(점검) 중 하나
- **date / title / body** : 보이는 글자라 자유롭게

> 소식을 안 알리고 조용히 고치기만 할 거면 이 단계는 건너뛰어도 돼요.

---

## 2. 버전 올리기 — `version.json`

파일은 딱 한 줄. 값만 **이전과 다르게** 바꾸면 돼요 (날짜+숫자 추천):

```json
{ "version": "2026-07-01.1" }
```

→ 앱을 켜둔 사람에게 "✨ 새 버전이 나왔어요" 배너가 떠요.

---

## 3. 캐시 번호 올리기 — `index.html`

파일 위쪽의 `?v=6` **세 군데**를 `?v=7` 로 (숫자만 1 올리기):

```html
<link rel="stylesheet" href="css/fonts.css?v=7">
<link rel="stylesheet" href="css/style.css?v=7">
...
<script src="script/app.js?v=7"></script>
```

→ 사용자가 바뀐 css/js(공지 내용 포함)를 확실히 새로 받아요.

---

## 마무리

VS Code 소스 컨트롤에서 변경된 파일을 스테이지(+) → 커밋 → 푸시.
**새로 만든 파일이 있으면** 변경 목록에서 꼭 + 를 눌러 같이 포함하세요.

### 요약
1. `app.js` 의 `NOTICES` 에 소식 한 줄 추가
2. `version.json` 숫자 변경
3. `index.html` 의 `?v=` 숫자 올리기
4. 커밋 → 푸시
