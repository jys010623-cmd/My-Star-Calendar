# 보류한 버튼 메모 (나중에 다시 넣을 수 있게)

2026-06-19. 상단 바(모바일 헤더)에 있던 **건의함·다크 모드 아이콘 2개를 뺐어요.**
대신 그 기능은 다른 곳으로 옮겨서 그대로 쓸 수 있어요:

- 건의함 → **공지사항 모달** 안의 "💬 건의함" 버튼
- 다크 모드 → **미니홈피 모드 모달** 상단의 "🌙 다크 모드" 버튼

뺀 상단 아이콘은 **삭제하지 않고 `index.html`에 주석으로 남겨뒀어요.** 다시 상단에 넣고 싶으면 주석만 풀면 됩니다.

---

## 되돌리는 법 (상단 바에 다시 넣기)

`index.html`의 `<div class="mh-actions">` 안에서 아래 두 블록을 찾아, 감싸고 있는 주석 표시(`<!-- ... -->`)만 지우세요.

### 1) 상단 건의함 아이콘
```html
<!-- [보류] 상단 건의함 아이콘 ...
        <a class="mh-icon" href="feedback.html" ...>...</a>
        -->
```
→ `<!-- ... ` 와 ` -->` 만 지우면 복구.

### 2) 상단 다크 모드 아이콘
```html
<!-- [보류] 상단 다크 모드 아이콘 ...
        <button class="mh-dark" onclick="App.toggleDark()" ...>...</button>
        -->
```
→ 마찬가지로 주석 표시만 지우면 복구.

---

## 참고
- 두 아이콘을 상단에 다시 넣으면, 공지/미니홈피의 버튼과 기능이 겹치게 돼요(둘 다 둬도 동작엔 문제 없음).
- 다시 넣은 뒤엔 `index.html`의 캐시 번호와 `version.json`만 올려주세요.
